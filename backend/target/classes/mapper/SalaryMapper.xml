<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.epms.mapper.SalaryMapper">
    
    <!-- 调用存储过程计算月度工资 -->
    <select id="calculateMonthlySalary" statementType="CALLABLE" resultType="java.util.HashMap">
        {CALL sp_calc_monthly_salary(#{month})}
    </select>
    
</mapper>

